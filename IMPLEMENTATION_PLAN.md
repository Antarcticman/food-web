# 嚐記 Implementation Plan

這份文件是功能與 UI 一致性的實作追蹤表。每次修改共用概念時，先更新資料來源，再逐一核對所有 UI 出口，避免只改到畫面的一部分。

## 0. 實作進度（2026-08-03）

| 模組 | 本機 React | 正式資料層 | 下一個驗收點 |
| --- | --- | --- | --- |
| 共用 Avatar／表情／裁切 | 已完成 | `profiles.avatar_recipe` 已建表 | Google 登入後保存角色設定 |
| 單頁評分與分數詞條 | 第二階段一屏舞台版已完成；2026-08-06 已改接正式 visit／dish／本人 rating | 自動儲存／Ready、安全公開進度與共編 RPC migration 已部署 | 以兩台手機驗證 Realtime、上下滑評分與失敗重試 |
| 動態菜色列／長按預覽 | 44px 指示列、左右換菜、短按方向動畫與長按滑動菜名預覽已完成 | dishes／dish_consumers 已建表 | 兩台真機確認長按門檻與滑動手感 |
| 人物＋桌面＋菜盤舞台 | 已接入 Affinity 的 table／plate／dome SVG，共用 2000×2000 座標與固定人物 | category／visual recipe migration 已於 2026-08-05 部署 | 替換正式菜色分層素材 |
| 本桌明細／排序／快速新增 | 已完成並接上正式 repository | 共編資料表、結構鎖定及新增／排序 RPC 已部署 | 兩道以上時驗證多人排序衝突 |
| 沒吃到／略過／整體評價 | 已完成；空菜單會鎖住 TOTAL 並直接引導新增第一道 | consumer 狀態與整體鎖定已建立；006 空菜單防護待部署 | 部署 006 後以新空桌驗證資料庫拒絕整體分與揭曉 |
| 揭曉／結果版本 | UI 待下一切片 | 原子揭曉、版本快照、Admin 重開稽核已建立 | 實作食物主角的揭幕動畫 |
| Google 登入／朋友名單 | Google-only 登入門與 profile bootstrap 已接上 | allowlist、Admin bootstrap 與 profile 權限已部署 | 已完成首位 Admin 實際登入驗收；下一步接個人資料編輯 |
| 菜單圖片辨識 | 互動入口已完成 | Gemini Edge Function 骨架已建立 | 金鑰設定、壓圖與真實菜單測試 |

截至 2026-08-06，`supabase/migrations/` 內前五份 migration 已部署到正式 Supabase `food-web` 專案；第六份 `202608060006_require_dish_before_overall.sql` 已建立，待由 Dashboard SQL Editor 套用。Google Provider、受邀 Admin、本機登入門、正式首頁與 visit 評分 adapter 已接線；新增／確認／沒吃到／恢復與重新載入已用正式餐桌驗證，Edge Function 仍依各節狀態個別判定。

## 1. 評分詞條架構

### 單一資料來源

- 餐點資料與型別：`src/types.ts`、正式版 Supabase `dishes`；本機範例在 `src/data/demo.ts`
- 料理類型：每道餐點的 `kind`
- 詞條資料：`src/data/ratingReasons.ts`
- 分數區間：`reasonBand(score)`，分為 `low`、`middle`、`high`、`top`
- UI 渲染：只由 `RatingStage` 搭配 `reasonsFor()` 產生，不在頁面寫死詞條

### 目前料理類型

| type | 用途 | 鑑別重點 |
| --- | --- | --- |
| `seafood` | 魚、貝、甲殼類 | 鮮味、腥味、熟度、彈性、海味搭配 |
| `meat` | 牛、豬、羊、鴨等主餐 | 熟度、肉汁、嫩度、焦香、醬汁 |
| `dessert` | 甜點、餐後小點 | 甜度、口感層次、香氣、溫度、收尾感 |
| `overall` | 整次用餐 | 服務、出餐節奏、氣氛、價值、再訪意願 |

### 新增料理類型時

1. 在 `ratingReasonProfiles` 新增完整的四個分數區間。
2. 每區最多四個精簡詞條，畫面最多可選三個，不另外堆疊「更多」層級。
3. 詞條必須能解釋分數，不重複總分本身。
4. 在 375px 手機版驗證不換行溢出。
5. 至少測試一個低分與一個高分狀態。

## 2. 人物頭像架構

### 單一資料來源

- 人物資料與型別：正式版 `profiles.avatar_recipe`；本機範例在 `src/data/demo.ts`
- Humation 組合與表情圖層：只由 `src/components/Avatar.tsx` 產生
- 人物與手繪場景的正式外框色固定為 `#241F1B`；此色為品牌繪圖常數，不屬於使用者可編輯資料。
- 群組人物：只由 `ParticipantStrip` 呼叫共用 `Avatar`，不可再寫字母圓框或複製裁切規則
- 個人／評分角色：使用同一位 participant 的 Humation 設定，再依用途套用大頭照或表情圖層
- 多人半身縮放、水平截底與個人大頭照裁切集中在 `src/styles/global.css`；驗收時所有髮型左右都必須留有實際像素邊界
- Humation SVG 由 `Avatar` 的單一 layout effect 掛載，React 不直接重寫內部 SVG；每次評分變化後仍需保留相同的 `viewBox`、尺寸 class 與圖層順序
- `profile`、`bust`、`stage` 三種 frame 各自固定尺寸；裁切 transform 只放在 `avatar-mount`，回彈只放在 `avatar-motion`，外框與舞台定位層禁止參與動畫
- 已驗收的 `profile` 裁切是圓框內的小型頭肩照：mount `scale(1.28) translateY(-3%)`。不得再以舞台或 bust 比例覆蓋；若要修改，必須同時比對頂部帳號與身分卡兩個出口
- 多人列禁止負 margin 疊放；pending 只使用灰階與降低透明度，不再疊加右下角勾勾／時鐘，並以 `3/6 已評` 文字提供非色彩狀態線索
- 每道菜的 `ParticipantStrip` 固定放在菜名右側，最多顯示五位；自己固定第一位，其餘依加入順序排列，評分完成時不得重新排序
- 第六位起以 `+N` 收合；點擊可見人物顯示姓名與狀態，點擊 `+N` 開啟完整進度 bottom sheet
- `not_eaten`、`skipped` 不列入人物列與分母；只有 `pending`、`completed` 會顯示

### UI 出口核對表

| 出口 | 顯示方式 | 狀態 |
| --- | --- | --- |
| 頂部個人資料 | 圓形大頭照 | 已統一 |
| 評分者身分 | 使用登入帳號，不另外建立重複身分卡 | 已統一 |
| 表情評分主角 | 同角色＋表情圖層 | 已統一 |
| 首頁同意／異議 | 半身＋個人底色 | 已統一 |
| 餐點評分進度 | 菜名右側最多五位半身＋`+N`＋完成數 | 已統一 |
| 菜單共編在線成員 | 半身＋個人底色 | 已統一 |
| 每道菜用餐者 | 半身＋個人底色 | 已統一 |
| 回訪建立動畫 | 半身 Humation | 已統一 |

`+9` 之類的數量徽章不是人物，保留文字圓章；點擊後必須提供完整姓名與文字狀態。

## 3. 每次修改的完成條件

1. 先用搜尋盤點相同概念的所有 DOM、CSS class 與 renderer。
2. 修改共用資料或 renderer，避免逐頁複製邏輯。
3. 重新搜尋舊 markup／舊 class，確認沒有殘留。
4. 驗證 375px、手機橫向與桌機版無水平溢出。
5. 驗證鍵盤操作、ARIA 名稱與 reduced-motion 不受影響。
6. 在本文件的出口表或類型表補上新增項目。

## 4. 評分草稿架構

- 第一階段本機 demo 儲存鍵：`tastelog.react-demo.v2`；第二階段目標為 `v3`，必須提供非破壞 migration，不能直接丟棄現有草稿。
- v3 儲存內容：所在菜色、每道菜揭蓋／未評／已評／沒吃到狀態、nullable score、詞條、視覺 recipe、TOTAL 與 Ready。
- 儲存策略：拖動過程只更新視覺，放手後保存 score；詞條連點使用短 debounce，切菜前 flush，避免逐 frame 寫入。
- 恢復策略：驗證版本與陣列長度後才套用；損壞或舊版資料直接忽略
- 完成策略：每道菜不提交；完成 TOTAL 後只做一次房間 Ready，結果收進用餐回憶後才清除草稿
- 正式版替換：保留相同 snapshot schema，將 `localStorage` adapter 換成登入帳號的同步 API

## 5. 單一評分頁與動態菜色列（第一階段基準，已實作）

> 本節記錄 2026-08-03 第一階段方向，保留作為功能與視覺比較基準。React 本機互動切片已完成；標有正式版規則的部分已建立 Supabase schema／RLS，但仍待建立專案後串接 Google 登入與 Realtime。若本節與第 6 節衝突，一律以第 6 節的第二階段規格為準。

### 資訊架構

- 取消「我的評分／本桌菜單」兩個頂部分頁；菜單是評分導航的一部分，不再是另一個管理頁。
- 評分主畫面保留目前菜色、表情滑桿、分數詞條與完成人數。
- 原本的圓點指示器升級為可水平操作的「動態菜色列」。
- 動態菜色列最左側是「本桌明細」入口，最右側固定為「＋ 快速新增」。
- 共編、分配、菜名修正與來源確認改放在本桌明細的 sheet／小 modal，不建立第二個頁面層級。

### 動態菜色列

- 非目前菜色維持小型圓點／縮圖，減少佔用空間。
- 目前菜色展開為較大的長圓形，顯示菜名與狀態。
- 短按菜色立即切換；按住約 180ms 並水平拖曳時進入連續預覽，放手後才確認切換。
- 預覽期間只移動展開中的長圓形，並在上方顯示菜名、縮圖、自己的狀態與完成人數；評分主畫面與未送出的分數不重繪。
- 若垂直位移先超過水平位移，取消預覽並交還頁面正常捲動。
- 拖到最左明細或最右新增入口時不自動開啟，必須另外點擊，避免誤觸。
- 點擊任一道可直接切換，左右菜色按鈕仍作為手勢替代操作。
- 最右側「＋」開啟快速新增 sheet；拍照辨識、貼圖／截圖與手動輸入由同一入口判斷。
- 最左側「本桌明細」開啟可掃描全部餐點的清單，避免把大量編輯控制塞進評分主畫面。

### 本桌明細

- 每列至少顯示菜名、菜單順序、自己的狀態與全桌評分完成數。
- 評分流程右側優先顯示 `4/7 已評`；價格放在菜名下方的小字，不搶走目前任務焦點。
- 預設是快速掃描清單；點列切回該道評分，點 `•••` 才進入編輯。
- 刪除採 soft delete 與復原；已有評分的菜不可直接硬刪。
- 草稿與尚未有人評分的餐點可由所有成員共編，並保留修改者、時間與復原紀錄。
- 第一筆評分送出後鎖定菜名、合併與刪除；一般成員只能提出修正或調整自己的「有吃／沒吃」，Admin 才能修改結構。
- 同一道菜被編輯時顯示在線編輯者並暫時鎖定；離開或 30 秒無操作後解鎖，Admin 可以強制接管。

### 快速新增

- 點菜色列最右側「＋」開啟單一 bottom sheet，不先要求選擇來源類型。
- 主要動作是「拍照／選擇圖片」；系統自動判斷菜單、下單截圖或收據。
- 同一個 sheet 直接提供菜名輸入框，補一道菜時按 Enter 即可加入。
- 圖片辨識先在上傳者的 sheet 顯示待確認清單，低信心項目才特別標示。
- 上傳者按「加入 N 道草稿」後才同步給全桌，避免錯誤辨識直接污染共編菜單。
- 公開後以「待確認」狀態顯示，任何成員可在第一筆評分前修正。

### 菜色排序

- 使用者希望能長按菜名並拖曳調整今天的出菜順序。
- 動態菜色列的長按水平拖曳只負責快速預覽，不承擔排序。
- 排序放在左側「本桌明細」內；長按菜名列或拖曳把手後上下移動，並提供「上移／下移」作為無障礙替代。
- 拖曳時目前列浮起、其他列平滑讓位；接近 sheet 邊緣時自動捲動。
- 評分前所有成員可排序；第一筆評分送出後只有 Admin 可調整。
- 排序只更新 course 順序；目前選取與評分資料綁定 dish ID，不因位置改變而跳錯菜。

### 整體用餐評價

- 心智模型類似一般表格最下方的 `Total`：固定在所有菜色之後、快速新增之前，視覺上與單道菜區隔。
- 評分場景以「完整餐桌擺設」代表整趟用餐：盤子、餐具、折疊餐巾與少量光點，不使用單一道菜或人物當主圖。
- 它是使用者主動提交的整體體驗分數，不由各道菜分數自動加總，也不進入菜色排行。
- 整體項目不出現在菜色排序 sheet，也不能拖曳。
- 自己仍有未評菜色時顯示鎖定與剩餘數量；點擊後導向第一道未完成菜色。
- 自己的菜全部完成、沒吃到或略過後才解鎖；提交整體評價後才算完成本次用餐。
- 菜色列順序固定為：`本桌明細 → 菜色 → 整體用餐 → ＋`。

### 漏菜與結果版本

- 揭曉前仍可新增漏菜；只有被分配吃到新菜的人從完成退回待完成，其他成員不受影響。
- 既有整體用餐分數保留；新菜評完後提示使用者維持或調整整體分數，不強迫重填。
- 自動揭曉會等待所有受影響成員處理完新菜。
- 揭曉後菜單與統計鎖定；只有 Admin 能重新開啟這次用餐。
- 重新開啟後產生新的結果版本，不覆蓋第一次揭曉，並保留修訂原因與操作紀錄。

### 晚到成員

- 晚到者在未揭曉前仍可透過附近餐桌、邀請連結、QR Code或六位房間碼加入。
- 加入後先完成精簡的「你吃了哪些？」sheet，再建立自己的評分滑動列。
- 全桌同套餐預選共享菜；各自點預設不選；混合模式預選共享菜、個人餐點待認領。
- sheet 每列只顯示菜名與縮圖，並提供全選／全不選，不放菜單編輯控制。
- 確認後才把晚到者加入評分母體與揭曉等待；其他人已送出的評分不受影響。
- 全員剛完成但尚未開始揭曉時，若有人加入則暫停揭曉並提示新成員。
- 已揭曉後不能直接加入舊結果，只能由 Admin 重新開啟結果版本或建立另一次到訪。

### 建立餐桌

- 建立時唯一必填是餐廳；以定位附近清單與搜尋選擇正確分店，找不到才手動新增名稱與地址。
- 選定後先檢查同餐廳的既有進行中房間；封測版同店同時只允許一桌，直接加入既有餐桌，不提供另開不同桌。
- 建立後立即產生六位房間碼、QR Code與分享連結，直接進入空的單一評分頁。
- 空菜色列顯示 `本桌明細 → ＋ 新增第一道 → 整體用餐（鎖定）`。
- 不在建立前詢問房間名稱、預計人數、用餐模式、價格、套餐版本、備註或房主。

#### 封測加入規則（2026-08-06 更新）

- Google allowlist 登入本身就是封測存取門檻；進行中的餐桌不再要求第二次輸入密碼或房間碼。
- 主頁直接列出所有尚未過期的 `active` 餐桌，整張餐桌卡皆可點擊；未加入者點擊後立即加入，已加入者點擊後直接回到評分進度。
- 加入採 idempotent 寫入，同一帳號重複點擊、重新整理或換裝置不能產生重複參與者。
- 首版尚未接定位時按建立時間由新到舊列出所有進行中餐桌；定位完成後才優先顯示附近餐桌，但仍保留查看全部進行中餐桌的入口。
- 六位房間碼保留在資料層，作為未來公開測試、分享或找不到餐桌時的備援；封測主流程不顯示、不要求輸入，也不稱為密碼。
- 點擊卡片後須在 100ms 內提供 pressed／loading 回饋；加入失敗時保留原卡片並提供明確重試，不跳到空白評分頁。
- 用餐模式在新增／辨識與分配菜色時推測並允許調整，不成為開房阻塞欄位。

### 草稿確認與評分解鎖

- 不設整桌的「開始評分」按鈕；菜色採逐道確認、逐道解鎖。
- 新增／OCR 菜色先以虛線與 `待確認` 出現在全桌滑動列。
- 點入後沿用正式評分卡的標題、場景與底部操作列；場景保留罩蓋並在右上顯示待確認狀態，避免另一套孤立版型。
- 頁首顯示菜名與來源預覽；任何成員可確認名稱或直接修正。
- 待確認時表情滑桿不可操作；確認後立即開放該道評分，不必等整份菜單完成。
- 第一筆評分送出後該道結構鎖定，之後只有 Admin 能重新命名、合併或刪除。
- 支援邊吃邊加菜：已確認的前菜可以先評，甜點與漏菜稍後新增。

### 揭曉後的個人分數（封測實驗）

- 封測版揭曉後預設顯示每位成員的 Avatar、姓名與原始分數，先用數次真實朋友局觀察效果。
- 這是可調整的實驗設定，不視為永久隱私原則；房間資料保留 `reveal_individual_scores` feature flag。
- 評分開始前必須清楚告知「揭曉後會顯示每個人的分數」，不能在揭曉當下才突然公開。
- 揭曉前仍維持完全保密；Admin 也不能提前查看或修改個人分數。
- 實測需記錄：是否有人因此不敢打低分、是否增加討論樂趣、是否有人要求下次匿名。
- `reveal_individual_scores` 由 Admin 全域封測設定控制，初始值開啟，不在每桌建立流程增加選項。
- 評分頁需同時顯示 `評分中保密` 與 `揭曉後會顯示姓名與分數`；第一次參與時再提供簡短說明。
- 設定變更只影響新房間；歷史結果保留建立當時的公開規則，不事後改寫。

### 已確認的用餐者狀態

| 狀態 | 暫定語意 | 暫定視覺 |
| --- | --- | --- |
| 未評分 | 有吃到、尚未送出 | 正常彩色／空心進度 |
| 已完成 | 已提交有效分數 | 彩色＋勾勾 |
| 沒吃到 | 不屬於自己的評分清單 | 降彩灰色，可點擊改成「我有吃」 |
| 略過 | 有吃到但明確不投票 | 淡色＋「略」標記，不計入平均但算完成流程 |

### 單道菜的成員頭像列

- 頭像列只顯示實際有吃、仍屬於這道菜評分母體的成員。
- 「沒吃到」與「略過評分」者不顯示在這道菜的頭像列，也不計入待完成人數。
- 有吃但尚未評分者保留頭像，使用待完成狀態。
- 已完成者使用完成狀態；不能只靠顏色區分，必須搭配右下角 icon 與可讀的 ARIA 狀態。
- 頭像列的分母是這道菜實際需要評分的人，不固定等於整桌人數。

### 沒吃到／略過入口

- 單道菜畫面只放一個低調但可見的 `不評這道` 動作，不同時堆兩顆按鈕。
- 點擊後以小型 bottom sheet 選擇 `我沒吃到` 或 `有吃，但略過評分`。
- 確認後自動前往下一道未完成菜色，並把該菜在滑動列標成灰色排除狀態。
- 以 toast 提供立即復原；之後點灰色菜色仍可選擇 `改成我有吃`。
- `沒吃到` 會移除自己的 dish consumer 關係；`略過` 保留吃過紀錄但不建立有效評分。
- 揭曉後不可直接更改，需透過重新開啟結果版本。

### 圖片與菜單 AI 辨識（延後，不列入封測 MVP）

- 第一版不永久保存使用者上傳的菜單、點餐截圖或明細原圖，也不把圖片存進 Google Drive。
- Humation Avatar 保存可重建角色的零件／參數，不另外保存合成後的頭像圖片。
- Avatar 編輯器允許更換頭型／髮型、身體／衣服、眼鏡與可用的髮色、膚色、衣服色；不得顯示或保存 stroke 色彩選項，渲染器永遠使用 `#241F1B`。
- 餐點照片與餐廳封面不是 MVP 必要資料；未來若加入相簿功能，再獨立評估物件儲存與圖片壓縮方案。
- 封測版先不串 Gemini、embedding、FAISS 或大型菜名資料集；十人朋友群的維護成本與用量管理目前不划算。
- 未來若恢復圖片辨識，圖片在手機端先做方向校正、裁切與縮圖，再透過受保護的伺服器端函式暫時送往 Gemini API；API 金鑰不得暴露在瀏覽器。
- Gemini 回傳結構化候選資料，至少包含菜名、價格、數量、來源區塊與信心度；低信心欄位在上傳者確認畫面標示。
- 圖片辨識結果必須由使用者確認後才同步；資料庫只保存確認後的結構化餐點文字與必要的辨識稽核資訊，不保存原圖。
- 辨識完成或失敗後即捨棄暫存影像；畫面提醒使用者先裁掉姓名、電話、付款資訊等不必要內容。
- AI 失敗、額度不足或使用者不願上傳時，永遠保留同一個 sheet 內的手動輸入路徑。

正式 MVP 暫定以 Supabase 承擔 Google 登入、Postgres 資料庫、Realtime 與 RLS 權限隔離；第一版不使用 Storage。最終採用前仍需完成前端框架、部署與備份策略的 grilling。

### 正式前端架構（已確認）

- 正式 MVP 採用 React + TypeScript + Vite，維持手機優先的單頁應用，不導入 Next.js 的伺服器渲染層。
- 現有 `index.html`、`app.js` 與 `styles.css` 視為互動與視覺原型；遷移時逐段轉成可驗收的共用元件，不直接一次性重寫全部畫面。
- Avatar、參與者狀態、菜色列、評分卡、詞條、bottom sheet、toast 與結果揭曉必須各自只有一個共用 renderer／component。
- TypeScript 型別必須涵蓋房間狀態、菜色狀態、用餐者狀態、評分生命週期、揭曉版本與 Admin 操作，不用鬆散字串在不同畫面重複判斷。
- Supabase client 僅使用可公開的 project URL 與 publishable key；所有資料存取仍由 RLS 保護。Gemini API 金鑰只存在伺服器端函式。
- 遷移期間先保留既有 demo 作為視覺比較基準；每搬完一個流程就完成手機尺寸、鍵盤、reduced-motion 與狀態一致性驗收。

### 部署拓樸（已確認）

- React + Vite 靜態前端在 MVP／封測階段部署至 GitHub Pages，先沿用使用者熟悉的 GitHub 工作流程；若使用 GitHub Free，發布來源 repository 需為 public。
- 封測先使用 HTTPS 的 `*.github.io/food-web/` 專案網址；Vite `base`、GitHub Actions build 與 Google OAuth redirect URL 需依此前綴設定。
- 未來若需要 private repository 免費部署、每個分支的獨立預覽網址、較完整的 redirects／headers 或更複雜的 SPA 路由，再評估遷移至 Cloudflare Pages；Supabase 資料與 schema 不受前端託管平台更換影響。
- Supabase Free 提供 Google Auth、Postgres、Realtime、RLS 與伺服器端 Edge Functions。
- Gemini 菜單辨識只能由 Supabase Edge Function 呼叫；金鑰存放於 project secrets，前端不得持有或記錄金鑰。
- 前端部署與後端函式分離：一般 UI 更新不需重新部署資料庫；Edge Function 也不承擔靜態網站託管。
- 封測目標維持每月固定成本 NT$0；任何需要綁定付款方式、升級方案或可能產生用量費用的功能，都必須先由 Admin 明確同意。
- 開發者負責環境變數、CORS、OAuth redirect URL、資料遷移與部署說明；使用者只需完成第三方帳號建立與必要的帳號擁有者授權。

### 封測備份與復原（已確認）

- Supabase Free 沒有自動每日備份；封測期間不因此升級付費方案。
- Admin 後台提供一鍵匯出完整 JSON，至少包含使用者公開資料、餐廳、到訪／房間、參與者、菜色、用餐者狀態、評分、結果版本與稽核紀錄。
- 匯出檔需包含 schema/export version、產生時間與資料筆數摘要，並提供相容性檢查；匯入／覆寫資料不列入第一版一般操作。
- 刪除預設採 soft delete，保留操作者、時間與復原入口；一般使用者不能硬刪已有評分或已揭曉的資料。
- 每次正式資料結構 migration 前先由開發流程建立資料庫快照／匯出；migration 檔案與回復步驟必須跟程式碼一起版本化。
- 備份不得提交到公開程式碼 repository；下載後由 Admin 自行保存。若日後資料量與重要性提高，再評估 Supabase Pro 的自動備份。

## 6. 375×812 一屏餐桌評分舞台（第二階段，本機切片已實作）

> 本節是 2026-08-03 經 `grill-me` 逐項確認後的新主規格。它會取代第一階段的「獨立俯視菜盤＋獨立人物滑桿＋每道菜提交」心智模型，但保留已驗收的 Avatar、參與者列、共編菜單、快速新增與明細 sheet。實作前先保存目前版本作為視覺比較基準，不直接在舊 CSS 上繼續堆疊例外。

### 6.1 新舊版本差異與淘汰範圍

| 第一階段 | 第二階段 | 遷移結果 |
| --- | --- | --- |
| 餐廳名稱、日期、地點與進度占用大型摘要 | 專注頂欄只顯示 `Lueur · 4/4` | 評分流程移除 `visit-summary`；完整資訊保留在主頁／明細 |
| 餐盤與人物分成兩個垂直區塊 | 人物坐在桌前，透視菜盤位於同一舞台 | `plate-art` 由 `DishVisual` 取代；人物外框與舞台合併 |
| 依手指在舞台中的絕對高度計分 | 方向鎖定後依相對拖曳距離調分 | 重寫 pointer gesture controller，避免按下瞬間跳分 |
| 只能透過菜色列切換菜 | 舞台左右滑換相鄰菜，指示列可直接跳菜 | 菜色列縮成 44px，保留非手勢替代路徑 |
| 進入菜色即有預設分數 | 未揭蓋顯示鐵罩；揭蓋後顯示 `—/100` | `RatingDraft.score` 改為可空值，禁止用 50／70 暗示 |
| 每道菜需要提交 | 第一次有效垂直滑動放手後自動建立評分 | 移除一般菜色的 submit action |
| `稍後再評`／`略過`需要額外按鈕或 sheet | 左右滑走就是稍後再評 | 移除 `skipped` 新流程與 `SkipDishSheet` 入口 |
| `沒吃到`在另一層選單處理 | 舞台原位切換 `沒吃到這道／改成我要評` | 不離開當前餐點，不使用跨頁復原 |
| 分數標頭、直向軌道與手勢提示各占一列 | 分數、評語與同步狀態整合進舞台 | 移除可見 score track 與常駐 gesture pill |
| 詞條常駐 | 垂直手勢放手回彈後才滑出單行詞條 | 手勢期間收起，不改變整頁高度 |
| 每道菜提交後才前往下一道 | 不自動切換；由使用者左右滑控制節奏 | 分數與詞條按菜色 ID 自動保存 |
| 每道菜有完成按鈕 | 只有完成 `TOTAL` 後有一次 `我評完了` | 完成動作改成房間層級 Ready |
| 完成數可直接觸發揭曉 | 全員 Ready 後才觸發揭曉 | 任一分數修改會取消自己的 Ready |
| 菜色使用少量固定圖案 | 自動分類＋可編輯的分層 `dish_visual_recipe` | 第一版用 CSS 幾何占位，日後換使用者繪製素材 |

### 6.2 一屏目標與版面預算

- 主要驗收尺寸：`375×812` 與 `360×800`；一般菜色必須在不捲動整頁的情況下完成揭蓋、評分、選詞條、沒吃到與換菜。
- 使用 `min-height: 100dvh` 與彈性舞台，不把總高度硬編成單一固定 px；瀏覽器動態工具列出現或收合時不可遮住內容。
- `320×568` 與橫向短螢幕啟用 compact mode：人物、桌面與間距縮小，允許詞條區少量垂直捲動；核心手勢與菜色切換仍不得失效。
- 所有尺寸禁止 document 水平溢出；bottom sheet 可以獨立捲動，但不可帶動背景頁。

| 區域 | 目標高度 | 保留內容 |
| --- | ---: | --- |
| 專注頂欄 | 48px | 返回、`餐廳 · 第 N/M 道`、登入頭像 |
| 菜色指示列 | 44px | 明細 icon、菜色指示、目前菜名、新增 icon |
| 餐點標頭 | 約 58px | `COURSE`、分類、最多兩行菜名、五位人物＋N、`3/6 已評` |
| 互動餐桌舞台 | 約 360–420px，依可用高度縮放 | Avatar、單色桌面、桌巾、鐵罩／菜盤、分數、同步狀態、沒吃到切換 |
| 動態詞條列 | 約 58px | 放手後顯示最多四個詞條，單行橫向滑動 |
| 安全區 | 系統決定 | `env(safe-area-inset-bottom)` 與少量留白 |

#### 首屏刪減規則

- 評分流程不常駐大型餐廳名稱、日期、地點與整桌摘要；完整內容只在主頁與本桌明細出現。
- 菜色指示列移除厚重說明外框、`明細／新增` 文字及常駐長按教學；第一次使用才顯示一次教學。
- 菜名下方不顯示 `待補充內容`。有食材、價格、來源或備註時，以菜名旁資訊 icon 開啟詳細內容。
- 評分頁不重複顯示登入者姓名或身分卡。
- `送出後暫不公開` 不常駐於每道菜；第一次進房及第一次完成評分前各提示一次，之後可從房間規則重看。

### 6.3 餐桌舞台與視覺圖層

#### 場景構圖

- Avatar 固定坐在舞台上半部；外框、人物容器與 `avatar-motion` 的幾何位置不得跟著分數或手指移動。
- 人物維持目前已確認的尺寸，只調整桌面水平線與裁切位置，讓人物自然坐在桌後；不得為了壓縮舞台再次縮小人物。
- 2026-08-03 校正：人物目前的水平與垂直定位皆已確認，不再因背景、桌巾或分數構圖移動人物本體。
- 桌面從人物胸口下方往畫面底部延伸，使用單一連續顏色，不做深淺分層；中央可保留低對比桌巾／餐墊。
- 菜盤、飲料與其他透明 PNG 固定錨定在人物正前方；素材可超出一般桌面安全框，不得被桌面容器裁掉。
- 左右換菜時只移動鐵罩／菜盤及必要的菜色文字，人物與桌面保持固定，避免整個畫面漂移。
- `score/100` 固定在舞台右上；分數動態詞句置於其下，不另建 `score-heading` 列。
- `同步中…／已更新／待連線同步／同步失敗・重試` 使用同色系淺色小字，不用連續 toast。
- `沒吃到這道` 固定於舞台左下；切換後相同位置改成 `改成我要評`。

#### Avatar 圖層保護

- 沿用 `Avatar.tsx`、Humation recipe、眼鏡遮罩、眨眼、嘴型、眉毛、星星與人物後方光暈，不生成或重畫人物。
- 上下評分只更新表情、眉毛、嘴型與高分特效；人物位置與尺寸的 `getBoundingClientRect()` 在手勢前後必須一致。
- 回彈只作用於 `avatar-motion` 的 scale，不可改變舞台定位；高分光暈仍位於人物後方，星星位於人物前方。

#### 第一版場景素材

- 禁止使用 AI 生成菜色或人物圖；先使用 HTML/CSS 幾何圖層建立桌面、透視盤、鐵罩與分類色塊。
- 每個圖層必須有固定資產插槽、viewBox／aspect ratio、錨點與安全區說明，日後可逐層替換透明 PNG／SVG 而不改互動程式。
- `prefers-reduced-motion` 時揭蓋、換菜、詞條與回彈改為短淡入／狀態切換，不做大幅位移。

### 6.4 菜色分類與分層 Visual Recipe

#### 基本分類

`vegetable`、`rice`、`noodle`、`bread_pizza`、`dumpling`、`meat`、`seafood`、`soup`、`drink`、`dessert`、`other`

- 系統依菜名自動推測 `dish_category`，但分類永遠可以在本桌明細手動修改；Admin 可修正所有資料。
- 第一版不為每道菜生成獨立插圖，使用分類配色與 deterministic seed 保持同一道菜跨畫面一致。
- 自動分類失敗或信心低時落到 `other`，不能阻塞新增或評分。

#### `dish_visual_recipe` 預留欄位

```ts
interface DishVisualRecipe {
  category: DishCategory;
  vessel: string;    // 餐盤、碗、杯、木板等
  base: string;      // 主體食物
  topping?: string;
  sauce?: string;
  garnish?: string;
  effect?: string;   // 熱氣、冰霧、氣泡等
  palette: string;
  seed: number;
  assetUrl?: string; // 使用者繪製的透明 PNG／WebP
  scale?: number;
  offsetX?: number;
  offsetY?: number;
  zIndex?: number;
}
```

- 第一版 UI 以 `category`、`palette`、`seed` 顯示 CSS 占位；同時實作 `assetUrl／scale／offsetX／offsetY／zIndex` 素材插槽，沒有資產時自動回退占位，不製作菜色編輯器。
- 所有素材共用「人物正前方」錨點，但可逐道調整縮放與偏移；PNG／WebP 使用透明背景與 `object-fit: contain`，允許大型菜盤、長杯或甜點架超出桌面標準框。
- 圖層順序固定為：桌面 → 容器／餐盤 → 主體 → 配料 → 醬汁 → 裝飾 → 熱氣／冰涼特效 → 鐵罩。
- 鐵罩是互動狀態層，不是菜色 recipe 的永久內容；`TOTAL` 場景不使用單一鐵罩。

### 6.5 手勢狀態機

#### Pointer session

```ts
type GestureMode = "idle" | "pending" | "vertical-rating" | "horizontal-dish";
```

- `pointerdown` 只記錄起點、目前分數與 active dish，不立刻改分或換菜。
- Pointer session 同時記錄是否從鐵罩開始；真實 pointer capture 後，未達拖曳門檻的鐵罩點擊由同一手勢狀態機直接揭蓋，不能再依賴可能被外層重定向的 `click`。
- 移動距離未達約 12px 時維持 `pending`；超過門檻後依主軸鎖定為垂直或水平，直到 `pointerup／cancel` 都不可中途換軸。
- 斜向移動無明確主軸時保持 pending，人物與餐盤不動；不得同時改分並換菜。
- 必須正確使用 pointer capture、`touch-action` 與 cancel cleanup；切換菜色或元件卸載後不可留下舊 pointer ID。

#### 垂直評分

- 使用相對拖曳距離，不使用手指在舞台中的絕對 Y；按下瞬間分數不得跳動。
- 約 160–180px 對應完整 100 分，並依實機手感調整；已評過從目前分數開始，首次評分使用不顯示的中性基準。
- 未揭蓋時垂直滑不建立分數；鐵罩只做小幅晃動並提示 `先揭開看看`。
- 放手時分數 clamp 至 0–100 的整數、觸發表情回彈、自動保存，並顯示對應詞條。
- 畫面移除可見直向軌道、圓點與常駐 `上下滑動` 膠囊；保留可存取的 range，讓鍵盤與輔助科技可調整。

#### 水平換菜

- 未揭蓋、未評、已評與沒吃到狀態都可左右滑到相鄰菜；水平滑不會自動揭蓋。
- 放手超過距離或速度門檻才完成換菜，否則菜盤回彈原位；人物與桌面全程固定。
- 滑走前自動保存目前 dish 的本機狀態；滑回時恢復該 dish 的揭蓋狀態、分數、詞條與沒吃到狀態。
- 首次完成評分後不自動前往下一道，使用者自行左右滑，避免選詞條時畫面跳走。
- 44px 菜色指示列保留點擊跳菜，作為不使用手勢者與跨多道快速導航的替代操作。

### 6.6 鐵罩揭蓋與評分生命週期

#### 單人／單道菜狀態

```ts
type DishRatingState = "unopened" | "opened" | "rated" | "not_eaten";

interface DishRatingSession {
  state: DishRatingState;
  resumeState?: Exclude<DishRatingState, "not_eaten">;
  score: number | null;
  selectedReasons: string[];
  openedAt?: string;
  ratedAt?: string;
  updatedAt: string;
}
```

- `unopened`：鐵罩蓋住餐點；輕點鐵罩才播放揭蓋。鐵罩使用單一底色與單一高光，不使用金屬漸層；揭曉只抬起罩蓋，菜盤保持固定。
- 鐵罩沿用目前已確認的顏色與圓頂比例；鐵罩托盤與料理容器必須共用同一條垂直基準線，不能以各自的 `top／bottom` 猜位置。
- `opened`：餐點已露出但尚未評分；顯示中性表情與 `—/100`，不計入已評人數。
- `rated`：第一次有效垂直滑動放手後建立；每次放手與選詞條都自動更新，無每道菜提交按鈕。
- `not_eaten`：不列入該菜分母與平均；保留 `resumeState`、舊分數與詞條但不參與統計，原位點 `改成我要評` 即可恢復。
- 每位使用者、每道菜只播放一次完整揭蓋；揭蓋狀態跨切菜、重新整理與裝置同步。其他人只知道已評／未評，不公開是否揭過蓋。
- `skipped` 不再是新流程狀態；左右滑走本身就代表稍後處理。既有 `skipped` 資料 migration 成未評狀態，不直接算完成。

#### 本機場景預覽料理

- 罩蓋測試盤已於好友封測前完整移除；正式畫面只保留實際餐點與整體用餐。

#### 手繪場景與料理素材

- 場景不再用 CSS 假裝正式美術；現階段只保留功能與圖層插槽。
- 桌子只佔場景下方約三分之一，不畫桌子外框、透視框線或 CSS 假桌巾。
- 背景、桌子、桌巾、料理容器、食物、裝飾、效果、鐵罩托盤與罩體皆使用可替換 PNG 圖層。
- 料理素材採 `容器 → 液體/醬汁 → 主食材 → 配菜/裝飾 → 效果` 疊加，並允許食物超出桌面或容器範圍。
- 完整檔名、畫布、圖層與品項盤點以 `HAND_DRAWN_ASSET_GUIDE.md` 為準。

#### 當前料理明細

- 菜名、內容描述與價格直接顯示在評分頁標題區，不再需要點菜名旁的資訊按鈕。
- 整桌明細仍保留為排序、共編與管理入口；它不是查看當前料理內容的必要步驟。

#### 自動保存與離線

- 垂直拖動期間只更新本機視覺，不逐 frame 寫入後端；`pointerup` 後立即保存 score。
- 詞條切換使用短 debounce 合併連點；切菜前 flush 目前 dish 的 pending write。
- 同步狀態：`idle → saving → saved`；離線為 `queued`，失敗為 `error`。`saved` 約一秒後淡出，`queued／error` 常駐直到處理完成。
- 本機 snapshot 升級為 `tastelog.react-demo.v3` 並提供 v2 migration；不得直接清除使用者目前 Demo 草稿。
- 正式版以使用者 ID＋dish ID 做 upsert；Realtime 只向本人回傳未揭曉內容，不能把私人分數廣播給其他成員。

### 6.7 動態詞條

- 垂直調分期間詞條收起；放手回彈後依料理分類與分數區間滑入。
- 每個區間最多四個精簡詞條、最多選三個、允許完全不選；維持單行橫向滑動，不建立更多層級。
- 再次垂直調分時先收起；新分數若跨區間，只保留仍有效的已選詞條，其餘移除。
- 選取或取消詞條立即自動保存，不需要額外提交。
- `TOTAL` 使用獨立的服務、出餐節奏、環境、價值與再訪意願詞條，不與單道菜混用。

### 6.8 菜色指示列與餐點標頭

- 指示列固定約 44px：左側明細 icon、中央菜色指示／目前菜名、右側新增 icon；移除常駐提示文字。
- 相鄰菜主要以左右滑舞台切換；點指示器可直接跳菜並顯示方向動畫。上方列保留 320ms 長按滑動預覽：按住後顯示菜名與序號、滑過圓點即時更新、放開切換；一般橫向拖動仍只捲動指示列。
- 菜色狀態至少能區分：未揭蓋、已揭蓋未評、已評、沒吃到、目前選取；不得只靠顏色，需提供可讀文字或 ARIA。
- 餐點標頭只保留 `COURSE`、分類、菜名與 `ParticipantStrip`；菜名最多兩行，右側人物不可被擠出。
- `ParticipantStrip` 沿用最多五位＋`N`、自己第一位、其餘加入順序；彩色為已評、灰階半透明為未評、沒吃到不顯示，並保留 `3/6 已評` 文字。
- 點人物顯示名字與已評／未評；點 `+N` 開完整名單 bottom sheet。禁止恢復右下角勾勾／時鐘。

### 6.9 `TOTAL`、Ready 與揭曉

- `TOTAL` 固定在所有菜色之後；自己的每道菜必須是 `rated` 或 `not_eaten` 才能進入。
- `TOTAL` 暫時使用空盤作為整體用餐主視覺；不使用單一道菜或鐵罩，也不進入菜色排行，正式插畫留待後續補上。
- 整體分數仍是獨立輸入，不由餐點平均自動產生；第一次有效滑動後自動保存，之後可隨時更新。
- 完成 `TOTAL` 後顯示唯一一次房間層級動作 `我評完了`；一般菜色永遠沒有提交按鈕。
- Ready 後其他人只看到頭像已準備，不看到分數；揭曉前修改任一道或 `TOTAL` 會自動取消自己的 Ready，需重新確認。
- 全員 Ready 才觸發揭曉。揭曉前預設完全保密；房間可用 feature flag 測試即時公開，但正式預設不得提前顯示他人數字或詞條。
- 揭曉後依房間建立時的 `reveal_individual_scores` 規則顯示平均、個人分數與詞條；Admin 也不能在揭曉前繞過 RLS 查看。
- 揭曉前新增漏菜時，只取消實際吃到該菜者的 Ready；完成新菜後再 Ready。已揭曉後仍採結果版本與 Admin 重開規則。

### 6.10 正式資料模型遷移

#### 前端型別

- `RatingDraft.score`：`number` → `number | null`
- `ParticipationStatus`：從 `pending／completed／not_eaten／skipped` 遷移為 `unopened／opened／rated／not_eaten`
- `Dish` 新增 `category` 與 `visualRecipe`
- 訪客／房間成員新增 `readyAt: string | null`
- 評分新增 `syncState`（僅前端 adapter 狀態，不直接作為公開房間資料）

#### 資料 migration 原則

- 舊 `completed` 且有有效 score → `rated`。
- 舊 `pending`：有本機草稿 score 則 → `opened`，否則 → `unopened`。
- 舊 `not_eaten` → `not_eaten`，保留可恢復資訊。
- 舊 `skipped` → `opened` 且 score 為 null，不列入已評。
- 既有 reveal/version/audit 記錄不得被 migration 覆寫；只更新尚未揭曉的 active visit。
- Supabase RLS 需新增「本人可 upsert 私人評分、其他人揭曉前只能讀狀態」測試；不能只依前端隱藏。

### 6.11 元件與檔案實作順序

#### 保留並重用

- `Avatar.tsx`：保留 Humation 與表情圖層，僅調整新版舞台 variant／定位。
- `ParticipantStrip.tsx`：保留排序、五位＋N、名字提示與完整名單。
- `BottomSheet`、`TableDetailsSheet`、`QuickAddSheet`：保留並配合新分類欄位調整。
- `ratingReasons.ts`：保留分數區間 API，擴充分類 profile。

#### 新增／重構

- `FocusRatingHeader`：48px 評分頂欄。
- `DishIndicatorRail`：44px 菜色指示列。
- `TableRatingScene`：管理人物、桌面、分數 overlay 與狀態出口。
- `DishVisual`：依 recipe 渲染透視容器、菜色層與特效。
- `ClocheLayer`：揭蓋、蓋回與 reduced-motion 狀態。
- `useRatingGesture`：封裝方向鎖定、相對分數、水平換菜與 cleanup。
- `ReasonTray`：放手後出現的單行詞條。
- `RatingSyncIndicator`：淺色小字同步狀態。
- `useRatingPersistence`：local v3／Supabase adapter、flush、offline queue。

#### 淘汰

- 評分流程的 `visit-summary`、重複身分卡與常駐 privacy note。
- 獨立 `plate-art`、`score-heading`、可見 `score-track`、`gesture-hint`。
- 一般菜色 `rating-actions` 的提交按鈕與 `SkipDishSheet` 路徑。
- 新流程的 `skipped` 狀態與「稍後再評」按鈕。

### 6.12 分階段交付

> 2026-08-06：第 1–6 階段的 React 切片、Google Auth、首頁與正式 visit 評分 adapter 已完成；本人評分、詞條、沒吃到、Ready、揭曉快照、新增與排序皆已接至 repository。第五份 migration 已部署；新增、確認、沒吃到、恢復與重新載入已在正式餐桌驗證。typecheck 與 production build 已完成，尚待兩台真機測試 Realtime、上下滑分數與完整 Ready／揭曉鏈路。

1. **資料契約**：新增 nullable score、狀態 migration、category／recipe、Ready；先寫 v2→v3 本機 migration 與單元測試。
2. **靜態一屏骨架**：實作專注頂欄、44px 指示列、餐點標頭與彈性舞台；仍可暫用舊評分 adapter，先驗證 375×812。
3. **菜色視覺與鐵罩**：加入 CSS 桌面、分類菜盤、鐵罩揭蓋與素材插槽；不碰 Avatar 內部 SVG。
4. **手勢狀態機**：加入方向鎖定、相對垂直評分、水平換菜、回彈與草稿恢復；逐項測試 pointer cancel。
5. **自動保存與詞條**：移除每道菜 submit、加入 sync indicator、離線 queue 與放手後詞條。
6. **沒吃到原位切換**：移除 skip sheet，加入 `resumeState` 與統計排除／恢復。
7. **TOTAL／Ready／保密**：整體場景、Ready 取消規則、全員揭曉 gate 與 RLS 測試。
8. **視覺與實機驗收**：320／360／375px、短螢幕、橫向、鍵盤、reduced-motion、兩台手機 Realtime。

每一階段必須可獨立回退；不得同一個 commit 同時重寫 Avatar、評分 gesture、資料 migration 與揭曉，避免無法定位回歸。

### 6.13 第二階段驗收清單

- 375×812、360×800：一般菜色的揭蓋、評分、詞條、沒吃到與換菜皆不需捲動 document。
- 320×568：核心舞台可用，只允許詞條區少量捲動；無水平溢出。
- 人物舞台 bounding rect 在 0、50、100 分及垂直拖動前後保持固定。
- 12px 前不觸發；鎖定垂直後不換菜，鎖定水平後不改分；pointer cancel 後可立即開始下一次手勢。
- 未揭蓋不可評分；輕點揭蓋不算已評；第一次有效垂直放手才建立 score。
- 未評顯示 `—/100`，程式與畫面都不存在預設 50／70 分。
- 左右滑走保留每道菜的揭蓋、score、詞條與沒吃到狀態；不跳確認視窗。
- 一般菜色沒有 submit 或「稍後再評」按鈕；`沒吃到／改成我要評` 在同一位置完成。
- 自動保存成功、離線 queue、失敗重試皆有非 toast 的文字回饋。
- 他人揭曉前只能讀已評／未評／Ready，無法從 API、Realtime payload 或 Admin UI 取得私人 score。
- 修改任一評分會取消 Ready；全員 Ready 才能揭曉。
- `ParticipantStrip` 五位＋N、不重排、不重疊、無右下 badge，所有髮型在 320／375px 均不被截斷。
- TypeScript typecheck、production build、console error、鍵盤操作、ARIA 名稱與 reduced-motion 全部通過。

## 7. 後續建議

### 2026-08-04｜Affinity 場景 SVG 接入紀錄

- 已直接引用 `affinity/table.svg`、`affinity/plate.svg`、`affinity/dome.svg`，不複製或改寫原始手繪檔。
- 三張圖統一由 `SceneArtwork` 以 `viewBox="0 0 2000 2000"` 與 `preserveAspectRatio="xMidYMid slice"` 疊合；人物位置維持原值。
- 固定層級：背景／人物 → `table` → `plate` → 料理素材 → `dome` → 操作提示與分數。
- `dome` 的 Affinity 原始 transform 保留；揭蓋動畫套在外層 motion group，以實際罩體的 bounding box 設定轉軸，不使用整張透明畫布的中心。
- 未揭蓋時盤子仍顯示、料理隱藏；揭開後才顯示料理。Demo 料理可點料理區蓋回，不需新增測試資料。
- 目前料理本體仍是分類色塊占位，但已改到盤子上層並與盤心對齊；正式料理 SVG／PNG 到位後只替換料理層。
- SVG 引用使用 `?no-inline`，確保 production build 仍輸出可使用 fragment ID 的獨立檔案。
- 動畫只改 `transform / opacity`、點擊區至少 44px，並保留 `prefers-reduced-motion` 降低動態版本。

- 菜單辨識後自動推測料理類型，但讓使用者一鍵修正。
- 一道菜可有主類型與次特徵，例如 `dessert + frozen`、`seafood + raw`。
- 先累積使用者常選詞條，再決定是否個人化排序；不要一開始就增加詞條數量。
- 整體用餐分數可在結果頁拆解為「餐點平均」與「用餐體驗」，但輸入頁仍維持一個總分。
## 8. 頒獎揭曉與餐廳歷史榜（2026-08-04 已完成本機互動切片）

### 8.1 本桌揭曉規則

- 所有人完成評分後才開放結果入口；第一版不做跨手機同步倒數，現場可自行喊「3、2、1」後各自點開。
- 每位使用者第一次進入時播放完整揭曉；看過後再進入直接顯示完整結果，另提供「重播揭曉」。
- 本次至少有 3 道有效餐點才播放排名揭曉；少於 3 道時直接顯示每道料理與整體用餐評價，不排名、不拉幕。
- 只有平均分 60 分以上能進前三名。沒有達標的名次直接標示「從缺」，不播放空名次動畫。
- 揭曉只需要一次操作：酒紅色左右簾幕拉開，第一名料理與分數進場，第二、三名橫列再依序出現；不使用三根頒獎柱、罩蓋或逐名點擊。
- 拉幕後採奶油白背景、淡金放射光、香氣線與少量星星；料理是唯一主角，不在結果主視覺放玩家 Avatar。
- 同分且評分人數相同視為並列；後續名次採競賽排名（例如 1、1、3）。兩道並列第一時中央並排；三道以上改顯示並列數量並在下方列出料理。
- 平均分只有一筆評分時顯示整數；兩筆以上顯示一位小數。
- 第一名主視覺只顯示菜名、平均分、評分人數與最多兩個共識詞條；第二、三名只顯示名次、小料理圖、菜名、平均分與評分人數。
- 前三名不在下方重複；「其他料理」從第四名開始使用無外框緊湊排行表。低於 60 分仍保留，僅以小字標示未入選。
- 點擊任一料理以 bottom sheet 顯示每位參與者的 Avatar、分數與詞條，主頁不做原地展開。
- 第一名落定時使用一次 30–35ms 輕微震動；不加音效。所有動畫均遵守 `prefers-reduced-motion`。

### 8.2 餐廳歷史排行榜規則

- 同一道菜可以在不同造訪中重複評分，原始每次造訪分數全部保留。
- 長期榜先計算「每個人對同一道菜的歷次平均」，再將不同人的平均分等權平均，避免常去的人因評分次數較多而壟斷榜單。
- 排行榜預設採全部歷史；「近期品質變化」先記錄為未來功能，不放進目前介面。
- 菜色正規化策略：同餐廳、完全相同名稱可自動連結；近似名稱需由使用者確認；明確不同版本則分開保留。
- 結果頁只顯示歷史榜入口與第一名預覽；完整榜單放在獨立頁，項目顯示平均分、評分人數與總評分次數。

### 8.3 本次實作範圍

- 新增本桌結果資料型別、排名／並列／門檻計算與可重現的本機多人示範資料。
- 新增精簡等待揭曉入口、單次左右拉幕、第一名主視覺、第二／三名橫列、名次從缺、重播與已看過狀態。
- 新增緊湊其他料理榜、個人評分 bottom sheet、整體用餐結論，以及依「個人先平均、使用者等權」計算的餐廳歷史榜獨立頁示範。
- 結果頁語境由餐桌狀態派生：現場揭曉使用「今晚」，從首頁「最近吃過」開啟的已結束餐桌使用日期＋`HISTORY`，並將標題、最佳料理、整體用餐與無障礙文字統一改為「這次／本次」；不得在歷史紀錄硬編「今晚」。
- 菜色視覺沿用可替換的圖層式 recipe；後續收到正式料理 PNG 後可直接替換結果主角，不改動排名或動畫狀態。
- 本機持久化新增 `resultViewed`，資料被重新編輯時會取消 Ready 與已揭曉狀態，避免顯示過期結果。
- 已驗證 320×568、375×812 與 667×375；沒有水平溢出，窄螢幕標題允許自然換行。
- 若瀏覽器或作業系統回報 `prefers-reduced-motion`，預設尊重並停用大幅動效；因評分遊戲以動畫為核心，頁面提供一次性的「恢復動畫」覆寫，選擇會保存於本機並同步到同源分頁。
- 結算前三名揭幕完成後，「其他料理」標題與料理列以短間隔依序上浮淡入，作為主秀後的收尾；不加入第二組聚光燈或大幅縮放，避免搶走第一名焦點。

### 8.4 結果資料層（契約與正式 migration 已完成）

- `result_versions` 的新版 snapshot 加入 schema version、ranking rules 與 canonical restaurant dish ID；既有結果版本不覆寫。
- 新增 `result_views`，以使用者＋結果版本記錄是否看過；重新開啟用餐產生新版本後可再次播放揭曉。
- 新增 `restaurant_dishes` 與 `dishes.restaurant_dish_id`；完全相同的正規化菜名可透過 RPC 自動連結，近似名稱仍必須由前端確認。
- 新增安全彙總 RPC：先算每個人對同一道菜的歷次平均，再讓使用者等權平均，不公開未允許揭露的個人原始分數。
- `src/lib/resultRepository.ts` 已提供 Ready、揭曉、讀取快照、標記已看過、連結 canonical dish 與讀取歷史榜的前端介面。
- 正式結果 migration 已於 2026-08-05 套用；Google Auth 與實際 visit ID 已於 2026-08-06 接上，尚待部署 005 並以兩台手機驗證 Realtime Ready；揭曉動畫仍各自播放。

### 8.5 正式資料庫部署紀錄（2026-08-05）

- 依序套用 `202608030001_initial_schema.sql`、`202608030002_table_rating_scene.sql`、`202608040003_results_and_restaurant_history.sql`、`202608050004_allowlist_admin_bootstrap.sql`，四筆交易皆成功提交。
- Google OAuth Provider、Site URL 與本機 redirect URL 已設定；受邀帳號已加入 allowlist並於 2026-08-05 完成首次登入，資料庫 profile 驗證為 Admin。
- Email provider 與匿名登入均已關閉；封測只保留 Google provider。沒有 allowlist 的 Google 帳號即使完成 OAuth，也無法建立 profile 或讀取任何應用資料。
- 13 張核心資料表均存在且啟用 RLS；每張表皆至少有一條 policy，`authenticated` 具有明確的最低必要 table grants。
- `anon` 對 `restaurants`、`ratings` 無 SELECT 權限；`authenticated` 可讀 `dish_consumer_progress`。
- `visits`、`visit_participants`、`dishes`、`dish_consumers` 共四張表已加入 `supabase_realtime` publication。
- 本次由 Dashboard SQL Editor 套用；日後接入 Supabase CLI 前，需以 migration repair 將四個版本標記為 applied，避免 CLI 重複執行既有 schema。

### 8.5 個人評分性格（僅記錄，尚未實作）

- 個人頁未來顯示吃過餐廳數、料理數、平均分、分數分布、常選詞條與長期評分性格。
- 性格分析需使用平均分、標準差、與同桌平均偏差、群體排序一致度及最低樣本數；不得只用單次或少量評分下結論。
- 概念方向包含「偏高分」、「偏低分」、「喜好分明」、「口味獨特」與「群體共識型」，正式稱號與友善文案留待專題 grilling。
- 資料層先保留可重新計算的原始評分，不在 `profiles` 永久寫死稱號；稱號演算法日後版本化。

## 9. 料理分類、對應素材與待修 UI（2026-08-07 更新）

### 9.1 確定要修

- 評分詞條膠囊內的文字不可換行；手機寬度不足時優先維持單列橫向滑動，不把詞條撐成兩行或增加過多首屏高度。
- 詞條區左側題目與右側膠囊需重新檢查最小寬度、間距及 320／360／375px 的顯示，避免題目被擠成逐字斷行。
- `TOTAL` 整體用餐評價仍缺正式主視覺；完成正式圖案前保留素材插槽，不把任一道單品料理當成整體用餐代表。

### 9.2 料理類型不能只用一個互斥分類

- 菜名來源可能是手動輸入、共編或圖片辨識；系統先依菜名與菜單上下文提出建議，最後必須讓使用者一鍵確認或修正，不能讓 AI 猜測直接成為不可修改的正式分類。
- 資料模型暫定拆成多軸：`course_role`（前菜／主菜／主食／湯品／甜點／飲料／小吃）、`temperature`（冷／熱／常溫）、`ingredient_family`（肉／海鮮／蔬菜／澱粉／乳品等）與 `format_or_technique`（生食／炸／烤／燉／麵／飯等）。
- 「飲料」「主食」屬於餐點角色，「冷／熱」屬於溫度，不應放在同一層互斥選項；一道菜可同時是 `dessert + cold + dairy`，也可同時是 `main + hot + seafood + grilled`。
- 正式 MVP 要討論並鎖定：第一層需要幾種 `course_role`、哪些軸由系統自動判斷、使用者修正入口放在哪裡，以及未知類型的保底樣式。

### 9.3 圖片與料理 Visual Recipe

- 對應圖片不只由菜名直接指定單張圖，而是由分類結果組成 `dish_visual_recipe`：容器底圖＋食物主體＋配料／裝飾＋可選效果；沿用 Avatar 多張透明 PNG 疊加的方式。
- 需要先盤點並繪製最小素材集合：盤／碗／杯／高腳杯等容器，以及肉、海鮮、蔬菜、飯、麵、餃、披薩、湯、甜點、飲料等主體。
- 系統依菜名提出 visual recipe；找不到精確圖案時退回同類型的通用色塊／通用料理，不顯示錯誤類型的具象食物。
- 待討論：容器與食物主體各需要幾種、哪些配料值得獨立圖層、顏色是否能套玩家主題色，以及使用者是否需要手動修正料理外觀。

### 9.4 詞條選擇規則

- 詞條採「少量共通詞條＋料理類型專屬詞條＋分數區段」組合，不為每個類型建立完全獨立且難維護的詞庫。
- 共通詞條處理所有料理都可能出現的感受；類型詞條只補足鑑別度，例如甜點的甜度／口感、飲料的香氣／濃淡、肉類的熟度／嫩度、炸物的酥脆／油膩。
- 正向、普通、負向詞條依分數區段切換；每次只呈現少量最相關選項，仍維持可多選與首屏低占用。
- 2026-08-12 第一版已接線：依 `course_role` 優先處理甜點／飲料／湯／主食，再依 `ingredient_families` 處理海鮮／肉／蔬食；每個分數區段固定組合兩個專屬詞與兩個共通詞，最多四個，保持單行橫向滑動。
- `TOTAL` 繼續使用完全獨立的整體用餐詞條，不受單品料理分類影響；既有歷史評分詞條不因日後修分類而被破壞性改寫。
- 待實測後討論：分數切段門檻、各類專屬文案是否夠自然，以及是否需要加入溫度／技法軸；封測回饋前不擴大詞庫。

### 9.5 `TOTAL` 整體用餐主視覺

- `TOTAL` 評的是整次體驗，不應使用單一盤料理；主視覺方向待討論為「完整餐桌／餐後桌景」，可包含餐具、數個空盤、杯子、菜單、小花瓶或燭台等元素。
- 桌上圖案應保持中性，不能讓 fine dining、便當、超商零食或飲料店看起來被錯誤限定成同一種用餐形式。
- 可評估依到訪類型切換少量桌景變體：多人正餐、單品／外帶、咖啡甜點、飲料／零食；第一版也可先只做一張通用桌景。
- 待確認使用者要補畫的檔案與圖層：桌景主體、桌上小物、整體用餐專屬揭蓋內容，以及是否沿用玩家主題色的桌巾／盤緣。

### 9.6 封測版分類改採「本機建議＋人工點選」（2026-08-12 第一版已實作）

- 使用菜名與選填備註跑本機關鍵字規則，只產生少量候選分類；不呼叫 AI、不產生費用，也不因判斷失敗阻止加菜。
- 加菜者直接點選建議膠囊確認或修正，確認後保存結構化分類；建議結果本身不是正式資料。
- 「評分理由詞條」與「料理分類詞條」必須分開儲存：前者屬於每個人的每次評分，後者屬於料理，可供圖片、專屬評分詞條與排行榜篩選共用。
- 分類至少拆成三軸，避免把不同概念塞在同一組：`restaurant_style`（fine dining／便當／超商等）放餐廳或到訪；料理第一版已實作 `course_role`（前菜／主菜／主食／配菜／湯／甜點／飲料／點心／其他）與最多三個 `ingredient_families`（肉／海鮮／蔬菜／蛋奶／米麵穀物／水果／豆類／菇類／綜合／其他）。
- 快速加菜維持單一入口：菜名為必填，價格與補充內容選填；分類候選採漸進展開。預設只露出最可能的 3–5 個選項，另提供一個「調整分類」入口，不放長下拉選單。封測期隱藏尚未實作的照片辨識入口。
- 未來排行榜可組合篩選，例如 `restaurant_style=fine_dining`、`course_role=dessert` 或 `ingredient_family=meat`；同一道菜允許同時出現在多個合理榜單。
- `dish_category` 暫時保留為視覺素材索引，不再承擔完整語意；`course_role` 與 `ingredient_families` 才是未來詞條和排行榜的正式篩選資料。
- `src/data/dishClassification.ts` 已建立本機加權同義詞規則；菜名與補充內容共同判斷，強詞可蓋過弱詞（例如「紅茶布丁」先判為甜點，不因紅茶誤判成飲料）。未知料理預設主菜並允許改為其他，不阻塞加菜。
- `QuickAddSheet` 已改成菜名優先、補充內容按需展開、少量建議膠囊與完整分類次級入口；所有選項可人工覆寫，主要食材最多三個。
- `202608120009_dish_classification.sql` 新增 dishes／restaurant_dishes 正式分類欄位、歷史資料回填、加菜 RPC 與餐廳料理連結同步；前端在 migration 尚未部署時仍可讀取舊欄位並退回舊 RPC。
- `DishClassificationPicker` 已成為快速加菜與本桌明細的共用元件；明細只常駐一行分類摘要，點鉛筆才在原列展開修正，不新增分頁。第一筆評分前一般成員可修正，評分後僅 Admin 可修正。
- `202608120010_update_dish_classification.sql` 新增可信任的分類更新 RPC、revision 紀錄、Ready 取消與餐廳料理同步；`protect_dish_structure` 也已把新分類欄位納入原有結構鎖。
- `matchesDishClassification()` 已作為圖片選擇與未來排行榜共用的 filter 邊界；下一步可在不重寫資料模型的情況下新增甜點／肉類等榜單。不要先蒐集大型菜名 dataset。

### 9.7 開桌定位與餐廳身分（2026-08-08 第二版已實作）

- 定位只能提供座標，不能單獨知道店名；封測版不串 Google Places，以避免新增 API、帳務與額度管理。餐廳來源只使用 10 位好友共用的 Supabase 餐廳目錄。
- 點擊「開始一桌」就立即請求目前位置；定位拒絕後不在一般流程重複打擾，只保留小型「重試」入口，且手動店名搜尋永遠可用。
- 附近搜尋半徑採 `max(300m, GPS 精度 × 2)`，上限 1km；空白輸入按距離列出附近舊店，輸入文字後只顯示正式店名／別名相近結果，先按文字相似度、再按距離排序。
- 每筆搜尋結果顯示正式店名、分店／地址、距離、歷史到訪次數，以及自己或哪些朋友吃過；選擇後先出現確認卡，必須再點「在這間開桌」。
- 高店名相似度且距離接近時標為「可能就是這間」，沿用既有餐廳是主路徑；仍保留「確定不同，建立新的」處理真正不同的店。
- 正式店名不會因使用者輸入簡稱、少字或錯字而被改寫；使用者確認既有餐廳後，輸入字串會保存為 `restaurant_aliases`，下次可直接搜尋。正式名稱只由 Admin 更改。
- 同品牌不同分店保留不同 `restaurant_id`，並預留 `restaurant_brands` 群組；只輸入品牌名時先建議附近分店，真的不清楚才允許建立未指定分店紀錄。
- 建立新餐廳時「儲存目前位置」預設勾選，清楚顯示 GPS 精度；取消後只建立名稱，不保存座標。使用者可只貼一次 `maps.app.goo.gl` 分享網址：由 Supabase Edge Function 讀取公開的 302 重新導向，不在瀏覽器跨網域抓 Google 頁面；解析重新導向中的店名、地址、座標與 `ftid` 後直接顯示單一確認卡，不再重複詢問店名、分店、地址或位置。解析失敗時才退回店名＋GPS／選填地址，不阻止開桌。
- 既有餐廳缺座標時，一般成員可在確認卡補寫第一次位置；已有座標只能由 Admin 修改。建立、補座標、正式名稱修改與合併都寫入餐廳稽核紀錄。
- Admin 可把重複餐廳可逆合併到正式餐廳：舊 ID 指向新 ID、歷史到訪移轉、舊名稱成為別名並保存 merge revision；必要時可依 revision 復原。
- 同一餐廳封測版同時只允許一個未過期 `active` 房間；開桌 RPC 使用 advisory lock，兩人同時操作也只會建立一桌，後到者直接加入。
- 新房間有效期改為 12 小時；超過 6 小時仍在進行會標示「可能已結束」，Admin 可手動關閉。揭曉後立即進入歷史，不等待有效期。
- 定位座標只用於本次餐廳確認與選擇性餐廳位置保存，不建立使用者移動軌跡。GitHub Pages 的 HTTPS 與 localhost 均可使用瀏覽器 Geolocation。
- 未來項目：使用人數增加後才加入「同餐廳、不同團同時開桌」；若要第一次到店就列出外部真實商家，再評估 Places provider，並保存 provider place ID 作主識別。
- 餐廳身分層保留可替換的外部 provider 欄位與接線邊界；未來改用 Google Places、OSM 或其他供應商時，只新增外部 ID／查詢 adapter，不改寫既有 `restaurant_id`、到訪與評分歷史。

### 9.8 自由評語與花費紀錄（2026-08-13 已確認方向，待實作）

- 每位使用者對每道菜與 `TOTAL` 都有獨立的選填自由評語；評語屬於該次、該人的 rating，不覆寫菜單「補充內容」，也不永久寫成餐廳事實。
- 評語 UI 放在分數詞條正下方，預設只顯示緊湊的 `＋寫一句`；點擊後原位展開 1–3 行輸入框，短 debounce／離開欄位時自動保存，不新增提交按鈕或另一頁。
- `ratings.note` 與 `score`、`reasons` 分欄保存；改分、跨分數區間更換詞條、取消 Ready、切菜、重新整理都不得清空評語。分數 upsert 不傳 `note: null`，評語更新也不重寫分數。
- 揭曉後的個人明細在姓名、分數與詞條下顯示評語；空白就完全不佔位置。評語第一版最多 300 字，可隨時回原評分位置修改。
- Musha 範例中「中辣其實不辣、多力多滋帶來不錯口感、牛肋條沒味道」是同一道菜的一則個人評語；「店員態度很差、找錢多找 100 元」屬於本次 `TOTAL` 整體用餐評語。
- 金額分層：既有 `dishes.price` 是共用菜價，放在本桌明細每道菜右側；明細最下方新增可覆寫的本桌帳單總額；`TOTAL` 評價區另提供每人選填的「我的實付」，避免共享帳單與個人花費混為同一欄。
